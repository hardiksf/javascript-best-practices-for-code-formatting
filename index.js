'use strict';

const city = 'San Francisco';
console.log(`city`, city);

fetch({
    method: 'GET',
    url: 'someUrl',
}).then()
    .then();

const sayHello = (hello) => {
    console.log(`hello`);
};

const myArray = [4, 4];

const myObject = { 'er': 2 };

if (false) {
    // do this
}
